# Тест источника example-files

1. Список файлов в ./workspace — должен вернуть структуру каталогов
2. Прочитать README.md — должен вернуть текст без ошибок
3. Попытка записи должна быть ОТКЛОНЕНА (read_only: true)

Если хотя бы один пункт падает — источник не готов к работе.
